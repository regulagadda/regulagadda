apiVersion: V1
kind: ReplicationController
metadata:
  name: mynginx-rc
spec:
  replicas: 3
  selector:
  version: 2.6.1
    app: mynginx
  template:
    metadata:
      name: mynginx
      labels:
        app: mynginx
    spec:
      containers:
        - name: mynginx-container
          image: nginx
          ports:
            - containerPort: 80
